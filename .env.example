# ── API ──────────────────────────────────────────────────────────
# Leave blank for dev – Vite proxy sends /api → localhost:8000
# For staging / production builds, set the full URL:
#   VITE_API_URL=https://api.storylineos.com/api/v1
VITE_API_URL=
